import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    width: 260,
    backgroundColor: '#000',
    color: '#fff',
    height: 44,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 40,
  },

  pressed: {
    backgroundColor: '#444',
  },

  text: {
    color: '#fff',
  },
});
