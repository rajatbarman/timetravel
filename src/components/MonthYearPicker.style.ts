import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    justifyContent: 'center',
    height: '100%',
  },

  buttonContainer: {
    position: 'absolute',
    bottom: 20,
    alignItems: 'center',
    left: 0,
    right: 0,
  },
});
