import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    width: 310,
    borderBottomWidth: 1,
    borderColor: '#999',
    height: 40,
    fontSize: 18,
  },

  inputFocused: {
    borderColor: '#333',
  },
});
